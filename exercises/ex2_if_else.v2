#!/usr/bin/env python3.8

#Exercise 2: Working with If/Else
user = {'admin': True, 'active': True, 'name': 'Nikos'}
prefix = ""

if user['active']:
    prefix += "ACTIVE - "
if user['admin']:
    prefix += "(ADMIN) "

print(prefix + user['name'])
